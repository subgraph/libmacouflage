#!/usr/bin/make -f
DPKG_EXPORT_BUILDFLAGS = 1
include /usr/share/dpkg/default.mk
export DH_VERBOSE=1
export DH_OPTIONS
export DH_GOPKG := github.com/subgraph/libmacouflage

%:
	dh $@ --buildsystem=golang --with=golang,systemd 

override_dh_auto_test:
	@echo skipped auto test
